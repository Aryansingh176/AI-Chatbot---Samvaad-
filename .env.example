# Copy this to a file named .env and fill values

# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://<user>:<pass>@cluster0.wwp9ao9.mongodb.net/usersdb?retryWrites=true&w=majority

# JWT secret for signing tokens (change this to a strong random string)
JWT_SECRET=change_this_to_a_strong_secret

# Server port
PORT=5000

# Frontend API URL (optional, for CORS)
VITE_API_URL=http://localhost:5000/api

# Groq API Key (if using AI features)
GROQ_API_KEY=your_groq_api_key_here

# Google OAuth (optional, if using Google sign-in)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
